name: Manual Approval

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Run ID'
        required: true

jobs:
  approval:
    runs-on: ubuntu-latest
    steps:
      - name: Approval
        run: echo "Manual approval required for deployment"